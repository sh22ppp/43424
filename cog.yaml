build:
  python_version: "3.10"
  python_packages:
    - "torch>=2.4.0"
    - "diffusers>=0.14.0"
    - "transformers>=4.48.0"
    - "accelerate>=0.20.0"
    - "huggingface-hub>=0.16.4"
    - "safetensors"
    - "replicate"
  gpu: true

predict: "predict.py:Predictor"
